

<form method = "post" action="/abandonorcomplete">
    <div class="input-group">
        <input type="hidden" id="taskId<%=item._id%>" name="taskId" value="<%=item._id%>">
        <input type="hidden" id="email" name="email" value="<%=email%>">
        <div name="checkboxNode" class="input-group-prepend">
            <div class="input-group-text">
                <input id="checkboxId<%=item._id%>" name="checkboxId<%=item._id%>" onChange="this.form.submit()" type="checkbox" class="check" data-toggle="toggle" aria-label="Checkbox for following text input">
            </div>
        </div>
        <input class="form-control no-line" type="text" placeholder="Owned by: <%=item.owner%>, Unfinished" readonly>
        <div class="input-group-append">
            <button type="submit" class="btn btn-primary claim">Abandon</button>
        </div>
    </div>
    </form>